import React from "react";
import Form from "./Form";
import Nav from "./Nav";

const Feedback = () => {
  return (
    <div>
      <Nav />
      <Form />
    </div>
  );
};

export default Feedback;
